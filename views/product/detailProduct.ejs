<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head')%>
<link rel="stylesheet" href="/css/productCreate.css" />
    <body>
        <%- include('../partials/header')%>
            <main>

                <div class="container">
                    <div class="titular">
                        <h2 class="titulo">Producto</h2>
                    </div>
                    <form action="/products/<%=product.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                        <article class="product-create-article">
                            <div class="icoExit">
                                <a href="/products">
                                    <i class="fas fa-times-circle"></i>
                                </a>
                            </div>
                            <div class="product-create-file">
                                <h2><%=product.name %></h2>
                                <img src="/Img/product/<%= product.image[0].name %>" alt="<%=product.name %>" class="detail-product_img">
                            </div>
                            <div class="setction">
                                <div class="product-create-category">
                                    <h2>Categoria :<%= product.category.name %></h2>

                                </div>
                 
                                <div class="product-create-descripcion">
                            
                                    <textarea name="description" id="descripction" cols="30" disabled
                                        rows="10"><%=product.description %></textarea>
                                </div>
                                <div class="product-create-row">
                                    <div class="product-create-stock">
                                        <h2>Stock:</h2>
                                        <input type="number" name="stock" class="edit-stock"  disabled value="<%=product.stock %>">
                                    </div>
                                    <div class="product-create-price">
                                        <h2>Precio:</h2>
                                        <input type='text' name="price" required placeholder="$" maxlength="6" disabled
                                            value="<%=product.price %>">
                                    </div>
                                </div>
                                <div class="btns">
                                    <button class="btn-agregar" type='submit'>Comprar</button>
                                </div>
                            </div>
                        </article>
                    </form>
            </main>

            <%- include('../partials/footer')%>

    </body>
    <script src="https://code.jquery.com/jquery-3.2.1.js"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $('#category > option[value="<%=product.category %>"]').attr('selected', 'selected');
        });
    </script>

</html>